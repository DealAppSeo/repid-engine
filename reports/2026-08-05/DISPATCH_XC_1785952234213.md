# Dispatch — XC

- agent: grok
- lane: L6 RED-TEAM — no write scope
- branch: claude/agent-dispatch-runner
- duration: 134s
- exit: 0

## Task

RED-TEAM, read-only, no code changes.

Read src/repid/penalty-provenance.ts in this repo.

It claims two properties. Try to falsify each and report [V] or [R]:

1. 'event_type can never upgrade an event into a more serious class' — evidence
   (hal_decision, hallucination_caught, contract_id) is checked before event_type.
   Find any input where a caller-controlled event_type alone produces a
   behavioural classification.

2. isBehavioral() deliberately EXCLUDES 'unclassified'. Find an input that SHOULD
   be behavioural but classifies as unclassified — i.e. a way for a bad actor to
   dodge a future HITL gate.

Be concise. If you cannot falsify a claim, say so plainly rather than padding.

## Output

I'll red-team `penalty-provenance.ts` read-only: load the lane spec, then try to falsify both classification claims with concrete inputs.Claim 1 already looks broken on a read-through — `INTEGRITY_TYPES` is type-only and runs before evidence. Checking writers and tests for real shapes that land in `unclassified`.Confirming with a direct eval of the pure classifier — no DB, no writes.# L6 RED-TEAM — `penalty-provenance.ts`

Read: `src/repid/penalty-provenance.ts` (+ tests, `repid-update.ts` event types). Pure eval via `ts-node` (no DB, no writes).

---

## Claim 1 — *“event_type can never upgrade into a more serious class; evidence is checked before event_type”*

**FALSIFIED [V]**

Order of checks (lines 106–118): dormancy → admin → **`INTEGRITY_TYPES` (type only)** → then HAL evidence → then `VALIDATION_FAILED`/`CHALLENGE_LOSS`.

Caller-controlled `event_type` alone, with no `hal_decision` / `hallucination_caught` / `contract_id`:

| input | class [V] | `isBehavioral` |
|---|---|---|
| `{ event_type: 'EPISTEMIC_VIOLATION', delta: -60 }` | `integrity_violation` (most serious) | **true** |
| `{ event_type: 'CHALLENGE_LOSS', delta: -50 }` | `challenge_loss` | **true** |
| `{ event_type: 'VALIDATION_FAILED', delta: -250 }` | `challenge_loss` | **true** |

Tests encode this: lines 82–87 assert all five integrity labels from type alone.

What the comment *does* get right: bare `HAL_SCORE_EVENT` without evidence → `unclassified` (not a type-driven HAL class). Counterparty upgrade still needs `contract_id`. The absolute claim (“can never…”) is false; integrity and challenge paths are type-only and behavioural.

---

## Claim 2 — *`isBehavioral` excludes `unclassified` → find should-be-behavioural → unclassified dodge*

**FALSIFIED (dodge exists) [V]**  
Exclusion itself is intentional and as documented [V]. The failure is coverage: real misconduct event types from this repo fall through to `unclassified` → `isBehavioral === false`.

| input (engine-real types) | class [V] | behavioural |
|---|---|---|
| `CONSTITUTIONAL_VIOLATION` (−60) | `unclassified` | **false** |
| `DEFENDED_DECEPTION_FABRICATED_CITATION` (−60) | `unclassified` | **false** |
| `HANDOFF_COSIGN_FALSE_PASS_SLASH` (−15) | `unclassified` | **false** |

`CONSTITUTIONAL_VIOLATION` is first-class in `repid-update.ts` / challenge scoring / badges / decay; `DEFENDED_DECEPTION_*` are the Trust Harness heavy penalties. `INTEGRITY_TYPES` has bare `'DECEPTION'` / `'SLASH'`, not the names the engine actually writes.

Secondary gap [V]: `veto_class` is on `PenaltyEventRow` but never read — `{ HAL_SCORE_EVENT, delta: -10, veto_class: 'high' }` → `unclassified`.

---

## Bottom line

| claim | result |
|---|---|
| 1. type cannot promote / evidence-first | **Falsified** — type alone → `integrity_violation` / `challenge_loss` |
| 2. unclassified exclusion is safe for HITL | **Falsified as a gate design** — production deception/constitutional/slash types dodge `isBehavioral` |

Not checked: live prod row distribution for those event types (SQL not run this pass). Logic falsification does not need it.

No code changes. No push/merge/DDL.

