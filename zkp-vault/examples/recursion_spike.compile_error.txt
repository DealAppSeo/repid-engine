VERBATIM compile error — the deliverable of the recursion spike.
Reproduce (local paths below sanitized to <CARGO_HOME> / <TARGET_DIR>):

    cargo build --example recursion_spike --features attempt_incircuit_verify

This is the EXACT missing capability at pin p3-* 0.3.0: there is no way to turn a
verifier's opened value (a degree-4 BabyBear extension-field element — a PCS opening
from an inner proof) into an AIR constraint expression, because `AB::Expr` does not
implement `From<Challenge>` and no in-circuit challenger/PCS exists to derive it
symbolically. That is what "verify a proof inside a proof" would require.

--------------------------------------------------------------------------------
error[E0277]: the trait bound `<AB as AirBuilder>::Expr: From<BinomialExtensionField<..., 4>>` is not satisfied
   --> examples/recursion_spike.rs:145:33
    |
145 |             builder.assert_zero(self.opened_value); // mismatched types: expected AB::Expr, found Challenge
    |                     ----------- ^^^^^^^^^^^^^^^^^ unsatisfied trait bound
    |                     |
    |                     required by a bound introduced by this call
    |
    = help: the trait `From<BinomialExtensionField<p3_monty_31::monty_31::MontyField31<BabyBearParameters>, 4>>` is not implemented for `<AB as AirBuilder>::Expr`
    = note: required for `BinomialExtensionField<p3_monty_31::monty_31::MontyField31<BabyBearParameters>, 4>` to implement `Into<<AB as AirBuilder>::Expr>`
note: required by a bound in `assert_zero`
   --> <CARGO_HOME>/registry/src/index.crates.io-.../p3-air-0.3.0/src/air.rs:100:23
    |
100 |     fn assert_zero<I: Into<Self::Expr>>(&mut self, x: I);
    |                       ^^^^^^^^^^^^^^^^ required by this bound in `AirBuilder::assert_zero`
    = note: the full name for the type has been written to '<TARGET_DIR>/debug/examples/recursion_spike.long-type-*.txt'
    = note: consider using `--verbose` to print the full type name to the console
help: consider further restricting the associated type
    |
137 |         fn eval(&self, builder: &mut AB) where <AB as AirBuilder>::Expr: From<BinomialExtensionField<p3_monty_31::monty_31::MontyField31<BabyBearParameters>, 4>> {
    |                                          +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++...

For more information about this error, try `rustc --explain E0277`.
error: could not compile `zkp-vault` (example "recursion_spike") due to 1 previous error
--------------------------------------------------------------------------------
