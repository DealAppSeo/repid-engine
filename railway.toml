[build]
builder = "nixpacks"

[deploy]
# startCommand = "node dist/index.js"
# ^^ Removed 2026-05-10: this overrode per-service dashboard start
# commands for receipt-indexer, proof-drain-worker. Each service
# now uses its own dashboard "Custom Start Command".
