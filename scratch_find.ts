import { extractHALSignals } from './src/hal/lib/extract'; for (let i = 1; i <= 100; i++) { const words = new Array(i).fill('word').join(' '); const res = extractHALSignals({ text: words, domain: 'test', certainty: 0.9 }); const score = (0.4 * res.harm_probability + 0.3 * res.epistemic_uncertainty + 0.2 * (1 - res.evidence_quality) + 0.1 * (1 - res.scope_appropriateness)) * (531441/524288); if (Math.abs(score - 0.3760616512298584) < 0.000001) console.log('WORDS:', i); }
