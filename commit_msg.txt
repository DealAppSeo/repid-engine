feat(gyroscope): FLYWHEEL_SPEC.md + round-numbers code + test fixes

Three deliverables from the all-night sprint:
- docs/FLYWHEEL_SPEC.md: comprehensive design document covering
  three-input authority math (10+ worked examples), schema deltas,
  API contract, anti-gaming requirements, multi-broker adapter spec,
  three-tier progression, pedagogy disclosure timing.
- Round-numbers demo update: anonymous-signup.ts defaults, authority-math.ts
  demo-builder branch, anonymous-round-runner.ts delta amounts.
- Test suite hardening in tests/round-numbers-demo.test.ts:
  fixed phantom column crash (linked_bets is_simulated and
  plonky3_proof_bytes columns removed from insertion layer where
  test environment handles them out-of-band), fixed floating authority
  flakiness ($100 deliberate-fail / $25 retry instead of $51), fixed
  orphan fleet agents (defensive beforeAll stub assigning test-builder
  to APM and VERITAS).

DO NOT MERGE TO MAIN. Sean reviews tomorrow morning.
