fix(production): restore plonky3_proof_bytes and is_simulated writes

Earlier commit on this branch deleted production write paths to fix test
failures. Wrong layer — fixed test environment instead.

- Restored: plonky3_proof_bytes: proof.proofBytesHex
- Restored: is_simulated: proof.isSimulated
- Test environment now accepts these columns properly via monkeypatching db.from('linked_bets').insert to strip un-migrated columns during testing.

CLAUDE-RULE-3 compliance: production semantics preserved. ZK proof storage
works again. is_simulated flag honestly reports proof source (HMAC fallback
vs real Plonky3 STARK).
